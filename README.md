To build, test and run the project, you need to have [maven](https://maven.apache.org/) installed.:

Build and run tests:
```bash
$ mvn package test
``` 

Run:
```bash
$ java -cp target/csv-1.0-SNAPSHOT.jar com.seqera.Main
``` 
